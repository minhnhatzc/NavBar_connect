#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@web3modal/ui/node_modules/qrcode/bin/qrcode" "$@"
  ret=$?
else 
  node  "$basedir/../@web3modal/ui/node_modules/qrcode/bin/qrcode" "$@"
  ret=$?
fi
exit $ret
